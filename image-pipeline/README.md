# 生图流水线

主展示版本采用 Node.js 生图任务编排，将文本流水线输出的场景提示词转换为可调度的图片任务，并规划场景锚点和人物阶段参考图。

## 核心能力

- 兼容多种提示词 JSON 结构并统一为标准任务；
- 按稳定场景编号校验、排序和补齐任务；
- 通过异步任务轮询获取生成结果；
- 按依赖关系调度场景任务；
- 复用角色阶段图和场景锚点，减少重复生成；
- 失败任务阻塞后续依赖，并支持定点恢复。

详细说明见[开发者指南](./developer-guide.md)。
